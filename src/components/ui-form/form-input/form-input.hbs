{{#if disabled}}
  <input
    class='form-input'
    type='{{type}}'
    name='{{name}}'
    placeholder='{{placeholder}}'
    required='{{required}}'
    id='{{id}}'
    disabled='{{disabled}}'
    value='{{value}}'
  />
{{else}}
  <input
    class='form-input'
    type='{{type}}'
    name='{{name}}'
    placeholder='{{placeholder}}'
    required='{{required}}'
    id='{{id}}'
    value='{{value}}'
  />
{{/if}}