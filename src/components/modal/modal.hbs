<div class='modal is-{{shown}}'>
  <div class='modal__wrap'>
    <h2 class='modal__title'>{{title}}</h2>
    <form class='modal__form {{class}}'>
      {{{input}}}
      {{{buttonSubmit}}}
      {{{linkCancel}}}
    </form>
  </div>
  <div class='modal__notification is-{{notififcationShown}} is-{{result}}'>
    {{modalNotification}}
  </div>
</div>